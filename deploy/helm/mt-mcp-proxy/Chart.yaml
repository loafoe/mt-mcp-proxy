apiVersion: v2
name: mt-mcp-proxy
description: A generic multi-tenant JWT/OIDC auth gateway that fronts any streamable-HTTP MCP server (e.g. github-mcp-server, mcp-grafana) with group-based tenant routing and per-tenant credential injection.
type: application
version: 0.4.0
appVersion: "0.3.0"
